cmake_minimum_required(VERSION 3.10)
project(axiompy VERSION 3.0.0 LANGUAGES NONE)

# AxiomPy is a pure-Python package built via hatchling/uv.
# This CMakeLists.txt is a placeholder for future C-extensions or
# mixed-language modules.  The build entry point is pyproject.toml.

message(STATUS "AxiomPy ${PROJECT_VERSION} — pure Python, no compilation required")
